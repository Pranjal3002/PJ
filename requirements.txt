numpy==1.24.2
Pillow==9.5.0
matplotlib==3.7.1
segment-anything==0.1.0  # Replace this with the correct version if different
tk==0.1.0  # tkinter versioning depends on platform; ensure it's compatible
